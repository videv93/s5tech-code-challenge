node_modules/
dist/
build/
coverage/
.env
.env.local
*.log
.DS_Store
# SQLite lives wherever DATABASE_URL points, which is the project root — not
# prisma/ — so match the files by extension rather than by directory.
*.db
*.db-journal
*.db-wal
*.db-shm
.vite/

# Prisma 7 emits the client into src/; regenerated by the postinstall hook.
src/problem5/src/db/generated/

# Wrangler local state
.wrangler/
