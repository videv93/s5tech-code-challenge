# Base URL of the Problem 5 API. Defaults to the deployed instance, so the app
# works with no configuration; point it at a local server to develop against one.
VITE_API_URL=https://api.duelcode.online
